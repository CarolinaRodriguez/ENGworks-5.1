
requires task.selection

dim VAV = inputbox("Enter VAV Number")

dim i = 1

while i <= task.selection.count
    dim item = task.selection[i]
    item.customdata["VAV"].value = VAV
    item.update()
endif
 
    i = i + 1
end while