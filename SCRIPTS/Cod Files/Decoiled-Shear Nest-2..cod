Rem This will prep the selected items so that they can be Nest for Shearing

If item.cid = 866 then
  if item.cuttype = "Machine Cut" Then
    if Item.customdata["Decoiled"].value = "Shear Nest" then
     item.Option["Override Cut Type"].value = "Decoiled Straight"
    endif
  Endif
Endif
Item.update()
