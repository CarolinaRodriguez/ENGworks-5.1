requires task.selection

dim i = 1

while i <= task.selection.count
  dim item = task.selection[i]
 If item.servicetype = "Valve" Then
  Dim Valve = item.alias
  Item.customdata["Valve"].value = Valve
  item.update()
 Endif
      i = i + 1
end while